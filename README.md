# my-app

Demo application. The authentication module (`src/auth.py`) enforces login
policy: account lockout, minimum password length, and session timeout.
Its behavior is documented in the docs site under "Authentication".
